% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ts_functions.R
\name{get_sample_acf}
\alias{get_sample_acf}
\title{Computes the sample autocorrelation of an ARMA(p,q) model}
\usage{
get_sample_acf(model, max_lag = NULL)
}
\arguments{
\item{model}{A list containing the output ARMA model from function \code{gen_arma()}}

\item{max_lag}{Integer. Maximum lag for which to compute the sample ACF.
Defaults to \code{floor(n / 2)}.}
}
\value{
A numeric vector of length \code{max_lag + 1} containing:
\deqn{\hat{\rho}(0), \hat{\rho}(1), \ldots, \hat{\rho}(\text{max\_lag}).}
}
\description{
Computes the sample autocorrelation function (ACF) of a time series
generated from an ARMA model. Values are obtained by normalizing the
sample ACVF by its value at lag 0:
\deqn{
  \hat{\rho}(h) = \frac{\hat{\gamma}(h)}{\hat{\gamma}(0)}.
}
}
\examples{
set.seed(42)
n <- 200
wn <- rnorm(n)
model <- gen_arma(n, wn, ar_coefs = 0.5, ma_coefs = NULL)

get_sample_acf(model, max_lag = 10)

}
